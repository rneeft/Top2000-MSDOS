name: DOS Build

on:
  push:
    branches: [ main ]

jobs:
  compiled_code:
    name: Push vbdoscompiler image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
    
      - run: docker pull rneeft/vbdoscompiler:latest
    
    
